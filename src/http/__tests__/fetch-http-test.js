/**
 * @author Kuitos
 * @homepage https://github.com/kuitos/
 * @since 2015-10-27
 */

import FetchHttp from '../fetch-http.js';

describe('FetchHttp', () => {

  it('fetch get', () => {

    // todo how to test ajax

    //FetchHttp.get('http://localhost:8080/posts/11', undefined, {cacheStore: true}).then(data => {
    //  expect(data.userName).toEqual('god12312');
    //});

  });

});
